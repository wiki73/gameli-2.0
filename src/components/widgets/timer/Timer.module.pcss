.timer {
  padding: 40px 0;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 16px;
}

.time {
  margin-bottom: 0;
  font-weight: 900;
  font-size: 48px;
  line-height: 58px;
  font-variant-numeric: tabular-nums;
}

.message {
  font-style: italic;
}
